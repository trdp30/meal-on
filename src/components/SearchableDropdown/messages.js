/*
 * SearchableDropdown Messages
 *
 * This contains all the text for the SearchableDropdown component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SearchableDropdown';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SearchableDropdown component!',
  },
});
